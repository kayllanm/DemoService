# Spring-Data-Rest-Seed
A starting point for a RESTful service using spring-data-rest, documented with Spring Data

# Getting started

```
docker-compose run --rm web mvn package
docker-compose up -d && docker-compose logs
```